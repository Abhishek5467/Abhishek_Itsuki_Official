Faded horizontal rule with an optional centered mono label.

```jsx
<Divider>// experience</Divider>
<Divider plain />
```

`align` positions the label (`start|center|end`); `plain` uses a flat hairline instead of the faded gradient.
