Quiet mono chip for tech stacks, tools, and filters.

```jsx
<Tag prefix="#">PyTorch</Tag>
<Tag interactive onClick={...}>OpenCV</Tag>
```

Lighter weight than `Badge`. Use `prefix` for a sigil; `interactive` adds a hover affordance for filter chips.
