Portrait image with optional photon gradient ring, glow, and status dot.

```jsx
<Avatar src="assets/abhishek-portrait.png" size={96} ring glow status />
<Avatar src="…" square size={64} />
```

`ring` wraps the photo in the cyan→violet gradient; `square` switches to a rounded square; `status` adds a green online dot.
